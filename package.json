{
  "name": "parkon",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npx vite",
    "build": "npx vite build",
    "preview": "npx vite preview"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
